telegram-message-bot
├── .github/
│   └── workflows/
│       └── send_message.yml   # يشغّل main.py كل ساعة تلقائيًا
├── main.py                    # كود البوت (بيتحقق من الوقت ويبعت الرسالة)
├── requirements.txt           # المكتبات المطلوبة
└── messages.txt               # قائمة الرسائل العشوائية
